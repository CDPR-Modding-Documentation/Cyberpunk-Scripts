class PerksLevelBarController extends inkLogicController
{
	protected editable var m_foregroundImage : inkWidgetRef;
	protected editable var m_backgroundImage : inkWidgetRef;

	public function SetProgress( progress : Float )
	{
		inkWidgetRef.SetScale( m_foregroundImage, Vector2( 1.0, progress ) );
	}

}

